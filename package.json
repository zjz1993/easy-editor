{
  "private": true,
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "pnpm turbo run clean --filter=@textory/* --concurrency=16",
    "start": "pnpm turbo run dev --filter=editor-demo",
    "build": "turbo run build --filter=@textory/*",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:publish": "changeset publish",
    "version": "changeset version && pnpm install --lockfile-only",
    "release": "turbo run build --filter=@textory/* && changeset publish",
    "pre:enter": "changeset pre enter beta",
    "pre:exit": "changeset pre exit",
    "release:beta": "turbo run build --filter=@textory/* && changeset publish",
    "check:external": "node scripts/check-external.mjs",
    "prepublish": "pnpm check:external"
  },
  "workspaces": ["packages/*"],
  "keywords": [],
  "author": "zhaojunzhe",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@changesets/cli": "^2.29.8",
    "@types/react": "^18.3.12",
    "esbuild-sass-plugin": "^3.3.1",
    "only-allow": "^1.2.1",
    "tsup": "^8.5.1",
    "turbo": "^2.3.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@9.13.0",
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
